# 2026-08-05 工作日志

## 业务知识双向联动 - 全量实施完成

### 背景
老大需求：系统各模块（需求/工单/会议/重点工作）与 Obsidian 业务知识双向联动，实现"按业务领域聚合工作流 + 知识自动沉淀回流"。

### Phase 1: 数据模型基础
- 新增 `pmwb_business_domain` 表（12 条政企业务线种子数据）
- 给 5 个业务表 + `pmwb_knowledge_item` 加 `domain_code` 列
- 新增 `GET /basic-data/business-domains` API
- Alembic 迁移 `20260805000001_business_domain_and_knowledge_linkage.py`
- 修复 `obsidian_link.py` 运营工单沉淀路径：`01-运营知识/` → `11-业务运营/`

### Phase 2: 需求+开发工单沉淀能力
- 新增 `sediment_requirement()`：需求→`10-业务建设/需求沉淀/`
- 新增 `sediment_dev_ticket()`：开发工单→`14-知识沉淀/开发交付/`
- 统一 frontmatter 模板 + `[[wikilink]]` 双向链接
- domain_code 写入知识索引和 Obsidian 笔记 frontmatter
- 新增 API 端点：`POST /obsidian/sediment/requirement/{req_id}`、`POST /obsidian/sediment/dev-ticket/{ticket_id}`

### Phase 3: 前端交互集成
- `BusinessDomainSelect.vue`：统一业务领域选择器（下拉搜索）
- `RelatedKnowledgePanel.vue`：相关知识侧栏（展示同领域 Obsidian 笔记）
- 6 个模块视图集成：RequirementView、OperationView、MeetingView、TicketView、KeyWorkView、KnowledgeView
- 知识中心新增"按业务领域浏览"视图（`DomainKnowledgeView.vue` + 路由）
- API 层新增 `getBusinessDomains()`、`sedimentRequirement()`、`sedimentDevTicket()`、`syncFromVault()`

### Phase 4: 反向同步+可视化
- 新增 `backend/services/vault_sync.py`：扫描 vault 目录中非索引笔记，自动建立知识索引
- 新增 `POST /knowledge/sync-from-vault` API
- 知识中心加"从 Vault 同步"按钮

### 验证结果
- pytest: 115 passed（3 个既有环境失败，非本次引入）
- vitest: 7/7 passed
- vite build: 干净
- 新增文件 5 个，修改文件 22 个
