# 2026-08-07

## 用户故事生成 UI 交互重构
- **问题**：旧版 split button + dropdown 设计混乱，用户找不到 Kimi 选项；LLM 生成期间无加载反馈，30s+ 等待期间无法判断请求是否发出
- **改造**：
  - 策略选择器：卡片式三选一（⚡合并生成/Kimi 智能拆分/📐按工作量拆分），每张卡片含图标+描述+状态标签，选中高亮蓝边框+✓标记
  - Kimi 卡片实时显示连接状态（已连接/未配置）
  - 生成按钮统一在策略选择器下方，按钮文案根据策略动态变化（"生成用户故事"/"Kimi 智能生成用户故事"）
  - 生成中：按钮变 loading + 实时耗时秒数（"Kimi 正在分析需求… 23s"）；故事面板显示加载遮罩+提示文字
  - 生成完成后：黄色提示条+确认落库按钮带数字角标
  - 已保存状态：绿色标签显示"✓ 已保存 N 条"
- **技术细节**：selectedStrategy ref 默认 rules_v2；storyGenLoading/storyGenElapsed 实时状态；setInterval 计时器在 finally 和 onBeforeUnmount 清理
- **验证**：vite build ✅ / vitest 7/7 ✅ / 端到端 HTTP Kimi 生成 30.9s, 2 stories ✅

## AI总结（WorkReport）模块重建 + 多 AI 共享工作树风险
- **背景**：AI总结模块（原「工作总结」已改名）最初在 `feature/kc-1-knowledge-linkage-fix` 开发，但该分支 commit 因本地 git 对象库损坏变为不可达；沙箱反复把当前 checkout 切到他人分支（如 `feature/sup-3-supervise-ui`），导致本模块文件从磁盘消失、菜单与接口 404/405。
- **重建**：从设计记忆重建全部文件（backend: models/schemas/4 services/router/main.py；frontend: api/router/view）；补 `pmwb_work_report.cc` 列（`ALTER TABLE ... ADD COLUMN cc TEXT` 修复 500）。
- **实测验证（2026-08-07）**：`GET /work-reports` 200；`POST /generate` 200；`DELETE /{id}` 200（原 405 已修复）；删后再查 400（确已删除）；前端 router 含 `meta:{title:'AI总结',icon:'EditPen'}`，菜单正常。
- **仓库外保险备份**：`D:/项目/_wr_backup/work-report/`（含 README 还原清单 + 4 处接线说明）。8 个核心文件为 untracked（`??`），models.py/router/index.js 为 AM——**尚未入库**，勿提交到他人分支。
- **⚠️ 多 AI 冲突升级**：当前工作树被切到 sup-3 分支，本模块文件仅存磁盘。建议老大：各 AI 用独立 clone 或仅 Vicky2号 做集成者统一入库，避免互相覆盖。
