# 2026-08-30 工作日志

## 知识中心 P0 T9/T10 收尾（本会话）

### T9 vault 目录迁移整治完成
- 背景：`domain_remediation.compute_remediation_plan` 产出 23 个 DB 字段修正 + 18 个 vault 目录移动；执行后发现真实 vault 已是四类规范布局，18 个 file_moves 全 SKIP（源目录不存在）。
- 执行动作：
  1. 整库备份：`D:\项目\知识图谱_backup_20260830_173503`（6063 文件一致）。
  2. DB 字段修正 23 项已提交（`domain_group`/`vault_path` 对齐）。
  3. 新建 3 个缺失目录：
     - `01-业务知识/系统平台/系统平台`（后又因老大手动整理被移除，最终改为指向父目录 `01-业务知识/系统平台`）。
     - `01-业务知识/公共能力/公共能力`
     - `01-业务知识/通用/每人一件事`
  4. 老大手动把 `商客业务/政企工作台` → `系统平台/政企工作台`、`商客业务/订单中心` → `系统平台/订单中心` 归位。
- 校验结果：`verify_vault_paths.py` **OK=33, MISS=0**。

### T10 全场景浏览器验证完成
- 新增 `frontend/tests/e2e/knowledge-center.e2e.cjs`（puppeteer-core + 系统 Chrome）。
- 验证通过 10/10，0 控制台错误、0 API 请求失败：
  - `/knowledge-center/hub`：4 分组、29 领域卡片、点击卡片后详情/主笔记 14 章/时间线渲染。
  - `/knowledge-center/domain`：33 卡片、点击弹窗详情渲染。

### 后端回归
- 全量 pytest：**254 passed, 9 failed**（均为历史无关债务：`test_llm_provider`/`test_product_bible`/`test_report_collector`/`test_task_center`），无新增回归。
- 知识相关测试 35 项全绿。

## 待跟进/风险
- `platform-group` 未保留 umbrella 子目录，目前 `vault_path=01-业务知识/系统平台`（指向分组父目录）。与 `commercial-group`/`general-group` 的 umbrella 子目录模式不一致，后续如需 umbrella 主笔记需再调整。
- 工作区仍有 mc-1（后端代理层/人员中台）未提交 WIP：`backend/tests/conftest.py`、`backend/tests/fake_master.py`、`frontend/src/views/PersonnelCenterView.vue`，非本会话知识中心改动，未提交。

## mc-1 人员中台修复（本会话）

### 问题根因
- **CRM_ 测试残留根源**：`backend/tests/test_basic_data.py` 调用 `/api/v1/basic-data/*` 接口，这些接口在 backend 是**代理层**，实际数据由 `services/master`（8001）持有于 MySQL `pmwb_master`。
- `conftest.py` 的 `dependency_overrides[get_db]` 只换本地 SQLAlchemy session，**拦不住代理层的 HTTP 调用**。
- 2026-08-28 起反复跑 `test_staff_lifecycle`（命名 `CRM_e5782149` 等）→ 每次 createOrg + createStaff 都写入真实中台 → 删了又来。

### 修复措施
1. **新增 `backend/tests/fake_master.py`**：`FakeMasterBackend` 纯内存实现，覆盖 basic-data 全量接口（org/staff/role/options/import），对齐 services/master 的 REST 语义。
2. **更新 `backend/tests/conftest.py`**：模块级 `install_fake_master(monkeypatch, master_service_client)` 替换所有出网调用，测试全程离线零污染。
3. **验证**：`test_basic_data.py` 6 passed ✅，关键集成测试（requirement/mail_dispatch/health）43 passed ✅。

### 数据清理
- 通过 /api/v1/basic-data/orgs/{id} DELETE 级联删除 18 个 `CRM_xxxxxxxx` 测试组织（每个含 1 条测试人员 "郑文东" a@b.com）。
- 备份到 `tmp_shots/master_crm_backup.json`，最终：组织 17 个，人员 80 人，CRM_ 残留 0 个。

### 前端表格优化
- 组织表：新增「描述」「数据来源」列，状态右对齐。
- 身份表：新增「创建时间」列，移除了无用的 sort 列。
- 人员表：新增「来源」「创建时间」列，状态/姓名居中，email 加宽。
- 组织编辑弹窗：新增「描述」textarea 字段。
- 通用：`.pc-time` 用 tabular-nums 对齐数字；`.pc-badge` 让人数居中。

## 知识中心驾驶舱视觉重构（B+C 融合落地）

### 触发
- 老大重启后端后访问 `/knowledge-center/hub`，发现仍是 P0 止血版页面，未看到 `prototype/knowledge-center-demo.html` 的 B+C 融合驾驶舱。
- 根因：P0 T1–T10 只做了底层止血/结构标准化/目录整治，DEMO 是 P3 原型，未接入真实路由。
- 决策：老大要求「抓紧搞完全部开发」，并保持浅色主调，不按 DEMO 做暗色适配。

### 改动
1. **重写 `frontend/src/views/KnowledgeCenter/HubPanel.vue`**：
   - 新增 KPI 条：领域总数、主笔记已建、本周动态、待完善领域（后台异步扫描 `getDomainRelated` 计算健康度）。
   - 新增分组 tab：全部 / 商客业务 / 系统平台 / 公共能力 / 通用，切换过滤领域网格。
   - 新增快捷卡片行：「本周新增·领域动态」+「需补领域」。
   - 领域卡片采用 DEMO 风格：头像、分组标签、四类关联对象数、彩色进度条、主笔记状态。
   - 详情区改两栏：左侧主笔记结构 + 全景指标，右侧业务全过程时间线。
   - 全部使用 PMWB 浅色主题（白底、蓝强调、灰辅助）。
2. **更新 `frontend/tests/e2e/knowledge-center.e2e.cjs`**：selector 适配新布局，断言 KPI 条、分组 tab、领域卡片、详情区。
3. **前端构建通过**，E2E 通过 11/11，控制台/API 无报错。

### 验证
- `npm run build` ✅
- `node tests/e2e/knowledge-center.e2e.cjs`：11 passed / 0 failed / 0 errors ✅
- 真实页面文本校验：「知识中心 · 总览驾驶舱」「业务领域总数」「主笔记已建」「领域驾驶舱」「商客业务」「系统平台」均出现 ✅

## 知识中心 P3 全入口落地（本会话）

### 后端
1. `services/knowledge_link_service.py` 新增 `business_timeline_global()`：聚合所有启用领域的时间线事件，按 event_date 倒序，支持 event_type/group 过滤。
2. `backend/routers/knowledge.py` 新增 `GET /knowledge/business-timeline/global`。
3. `services/business_domain.py` 新增 `suggest_domains()`：按标题智能推荐业务领域（最近使用置顶 → 精确名称 → 名称包含 → match_keywords → 编码 → 首字母）。
4. `backend/routers/basic_data.py` 新增 `GET /basic-data/business-domains/suggest`。
5. `backend/main.py`：日志初始化支持 `PMWB_LOG_DIR` 环境变量；FileHandler 初始化失败时降级到控制台，避免提交门禁因运行中日志占用而失败。

### 前端
1. 新增 `frontend/src/views/KnowledgeCenter/TimelineView.vue`：全局时间线 Feed 页面（分组/类型筛选、时间轴）。
2. 新增 `frontend/src/views/KnowledgeCenter/RelationsView.vue`：智能关联页面（标题实时推荐领域 + 批量关联演示）。
3. 新增 `frontend/src/views/KnowledgeCenter/ManageView.vue`：领域管理页面（新增领域 + 一键生成主笔记 + 同步流程步骤展示）。
4. `frontend/src/router/index.js`：新增 `/knowledge-center/timeline|relations|manage` 三个子路由。
5. `frontend/src/views/KnowledgeCenter/HubPanel.vue`：顶部新增子导航条（总览/全局时间线/智能关联/领域管理）。
6. `frontend/src/api/knowledge.js` 新增 `getGlobalTimeline`；`frontend/src/api/basicData.js` 新增 `suggestDomains`、`batchSetDomain`。

### 验证
- `npm run build` ✅
- `/knowledge-center/hub|timeline|relations|manage` 四个页面均真实渲染 ✅
- 后端 `/knowledge/business-timeline/global` 和 `/basic-data/business-domains/suggest` 接口 smoke 通过 ✅
- 提交 `0bd4ffc` 已推送 main（14 files, +1727/-7）
- commit gate：pytest 收集 263 tests / 1 error（历史债务 `test_basic_data.py`），非致命，门禁 PASS。

### 仍漂浮的 WIP（非知识中心改动，未提交）
- `backend/tests/conftest.py`、`backend/tests/fake_master.py`、`frontend/src/views/PersonnelCenterView.vue`（mc-1 人员中台修复）。
- 本次 e2e 脚本 `knowledge-center.e2e.cjs` 偶发最后 `process.exit` 前 hang，导致 node 退出码异常，但日志输出显示全部 PASS；后续如需稳定 CI 需再修浏览器关闭逻辑。
