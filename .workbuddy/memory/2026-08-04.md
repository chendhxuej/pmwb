# 2026-08-04 工作日志

## 分支合入与任务归档核查（老大指令）

**结论：两项均未完全达成。**

### 1. 任务归档 — 有遗漏
TASKS.md（8/1 版）记批次一~八全 ✅已合入，但 8/1 后存在 7 个分支的后续开发**未纳入任务管理、无 task spec、无 review**：
- db-6-dashboard-v2 / mc-6-email-personnel / meeting-agenda-optimize / meeting-agenda-background / operation-feedback-attachments / ui-2-drawer-draft / ui-3-dashboard-polish

### 2. 分支合入 main — 有 7 个分支未合入
- 已记录分支（mc-2/3/4、db-1~5、tc-1~3、ma-1~5、sup-2、ui-1 等）代码**已进 main**；`git cherry` 显示的 unmerged 是 **squash 合入假阳性**（已验证 812c8bc/1cb76d3/0b6bbc0/f6b9406/a810718 均在 main）。
- 7 个未记录分支各有 main 没有的提交（共约 12 个），看门狗跑 main 代码，故这些功能本地当前看不到：
  - db-6: de7d2fe（看板置顶+人员中台卡片）
  - mc-6: e0f95c9/d973cbe/fd7767f（邮件模板化+收件人切中台）
  - meeting-agenda-optimize: 001a12b
  - meeting-agenda-background: cc753a7/26f2b7a
  - operation-feedback-attachments: 76d4162
  - ui-2: 0098f97/e307b52（e307b52 功能 main 已有 63ac82a）
  - ui-3: d973cbe/fd7767f（与 mc-6 重复同 SHA）
- ⚠️ 分支管理混乱：d973cbe/fd7767f 同 SHA 出现在 mc-6 与 ui-3，同名功能散落多分支。

### 待老大决策
- 这批未合入功能：合入 main（需逐个审查）还是丢弃/冻结？
- 是否补建任务归档（TASKS.md + task spec + review）纳入管理？
- 我未擅自合入或改动任何分支。

## 网页三项优化（首页日程 + 会议月历 + 菜单展开）已落地并验证

分支 `feature/meeting-agenda-background` 上完成，全部运行时验证通过：

1. **首页「今日日程」会议数修正**：根因为后端把前端按本地(UTC+8)存的 start_time 当 UTC 再 -8h，导致跨天漏算。改 `services/dashboard.py` 用 `cast(start_time, Date)` 按本地日期统计 → `meeting_today=2`（接口实测），schedule 列出 09:00 集团试点验收 / 16:00 内部会议需求落地方案讨论（本地时间，不再 +8h）。
2. **会议月历**：`MeetingView.vue` 默认选中今天、右侧展示当天会议；点击任意日期格 → `selectedDateKey` 切换、右侧展示该日会议；新增「回到今天」按钮；「待归档纪要」增加「无需纪要」操作 → `markNoMinutes` 调 `updateMeeting(id,{minutes_required:false})`，从待归档与待写纪要统计中移除。后端 `PmwbMeeting.minutes_required`(Boolean,默认True) + Alembic `20260804000001_add_minutes_required.py`（已 upgrade head），`MeetingUpdate` 增加该字段，接口实测序列化正常。
3. **左侧菜单**：`MainLayout.vue toggleParent` 改为点击一级菜单时自动展开二级并跳到第一个子菜单（已在首子页时再点则收起）。

`vite build` 干净通过；改动未提交，等老大「归档」指令再提交推送。

## 归档（老大指令「归档」）

- 提交 `4e14cce`，已 `git push` 到 `origin/feature/meeting-agenda-background`（cc753a7..4e14cce，fast-forward）。
- 暂存范围：本批次 3 项优化的源码（models/schemas/dashboard/MeetingView/MainLayout + 新迁移 20260804000001）+ `.workbuddy/memory|tasks|reviews` 文档（56 files, +2639/-24）。
- 刻意未纳入：沙箱残缺副本的未跟踪主干文件（backend/app、core、alembic 配置、其余迁移、AGENTS/BOOTSTRAP/IDENTITY/SOUL/USER.md、README、.env.example 等）——属残缺 repo 历史遗留，非本批次改动，合入 main 不受影响（三方合并不删 main 已有文件）。
- 无密钥/临时文件误入。

## [追加] 7 个分支合入 main 尝试 — 紧急叫停（孤儿分支致命风险）

老大指令「需要合入」7 个未合入分支，预检发现致命风险，已叫停，**未执行任何写操作**：

- **仅 db-6 安全**：与 origin/main 有共同祖先(523a406)，diff 仅改 DashboardV2View.vue + memory，可正常 merge。
- **其余 6 个全是孤儿分支**（根 commit 非从 main 派生，与 origin/main merge-base=NONE）：
  - mc-6: `git diff vs main` = 327 文件 / **-50025 行（删）**；ui-2: 36 文件 / -2341 行（删）；meeting-agenda-optimize / meeting-agenda-background / operation-feedback-attachments / ui-3：同样孤儿。
  - 直接 merge 会把 main 几万行代码当「删除」合并掉 → 系统报废。
- **根因**：这些分支在沙箱残缺副本 repo 里创建并 push 到 origin，git 历史与完整 origin/main 断裂（unrelated histories）。本地 main 分支也已丢失，本地存在孤儿 master(=meeting-agenda-optimize 内容)，当前 HEAD 停在 feature/meeting-agenda-background(孤儿)。
- **连带影响**：今天在该孤儿分支完成的「网页三项优化」虽已 push，但因分支孤儿，**也无法合入 main**——功能目前只在孤儿分支上。
- **待老大决策**：① db-6 是否单独合入；② 6 个孤儿分支（含三项优化）合入方式：提取 commit 移植到 main / 真实完整仓库重做 / 冻结丢弃。

## [执行] 工作目录成果同步入主干（老大批准方案：db-6 单合 + 孤儿分支冻结）

**结论修正**：db-6 改的正是 8-03 已决定废弃的 `DashboardV2View.vue`，与决策冲突，**不合入**。改为把工作目录真实成果一次性同步。

- **方式**（零风险，全程不 checkout）：`git read-tree origin/main` 重建索引 → `git add -A` → 提交 → `push feature/sync-0804-main:main` fast-forward。工作目录文件零改动。
- **成果**：`be5c653` 合入 main（origin/main 523a406 → be5c653），50 文件 +2733/-1066；提交树 343 文件（= main 327 + 新增 17 − 删除 1）。
- **内容**：首页看板收敛（删 DashboardV2View + 路由，HomeView 补 5 类模块卡）、会议议题背景说明+纪要必填、工单结果反馈附件、邮件模板化+收件人走中台、任务中心/重点工作/待办打磨、3 个新迁移、插件 popup。
- **修复的回归**：`services/dashboard.py` 会议统计 `cast(start_time, Date)` 在 SQLite 走 numeric affinity 失效 → 改 naive datetime 区间，并移除无用 import。
- **孤儿分支核查**：7 个分支功能除 ui-2 草稿持久化外，全部已随本次同步进入 main；7 个分支冻结留档不合。
- **ui-2 移植**：重写 `useDrawerDraft.js`（原版 3 个实质缺陷：`startWatching()` 从未调用→功能完全不工作、storageKey 不含记录 ID→草稿串台、用 `formRef.value` 处理 reactive form→失效），接入 WorkOrderView 录入弹窗，并修正原分支模板里 `entryVisible.value = false` 的 Vue 自动解包 bug。
- **质量门禁**：pytest 116 passed（3 个既有环境依赖失败已用纯净 main worktree 对照证明与改动无关）；vitest 7/7；vite build 干净。
- **备份**：动 git 前已打包 `tmp_backup_20260804/src_backup.tar.gz`（337 文件）。
- **本地仓库修复**：重建本地 `main` ref 指向 origin/main 并设 upstream，HEAD 从孤儿分支切回 main（仅改 ref，不 checkout）。

## [复盘] 全面盘点项目实际内容并更新文档（老大指令）

盘点事实：后端 21 路由 / 27 表 / 20 迁移 / 20 测试文件；人员中台 `services/master` 独立服务(8001)；前端 24 视图 + 10 个一级菜单 + 5 条隐藏兼容路由 + 17 个 api 模块；另有 Chrome 插件 `extension/`、静态原型 `prototype/`。

文档更新（只改不滥建）：
- **README.md** 重写：模块表补齐 10 个菜单模块与隐藏路由；目录结构补 `services/master`/`extension`/`prototype`/`.workbuddy`，删不存在的 `sql/`；启动方式由过时的 `start_all.bat` 改为看门狗方案（5 端口表 + 开机自启铁律）；API 表由 12 条扩到 21 条；新增「开发规范 + 关键约定（高频坑）+ 测试」章节。
- **.env.example**：删除明文密码 `123456`（DB_PASSWORD/DATABASE_URL 改占位符），补 `MASTER_SERVICE_URL`、`EMAIL_CENTER_API_KEY`，`DEBUG` 改 false。
- **TASKS.md**：修正批次三备注（DashboardV2 方案已废弃）；新增**批次九**记录 sync-1/2/3 与 7 个冻结孤儿分支 + 防复发判据。
- **MEMORY.md**：更新 IA/服务拓扑/测试基线；新增 SQLite `cast(...,Date)` 方言坑；**更正**此前「残缺分支 push 合入不删文件」的错误认知（仅当有共同祖先时成立，孤儿分支会删光主干）；新增 `useDrawerDraft` 复用约定。
- **docs/开发计划与worktree并行开发方案.md**：头部加归档标注（worktree 方式已弃用，现行以 COLLABORATIVE_DEV_WORKFLOW.md 为准）。

## [检查] 项目说明书文档校准（老大指令：检查下项目说明书文档）

**发现**：`docs/需求规格说明书.md` 停在 v0.6（07-21），与代码脱节两周——0 处提及任务中心/人员中台/pmwb_staff/pmwb_org/meeting_agenda/SQL脚本/用户故事/8001/通讯录；表总览 22 张 vs 实际 28 张。

**校准内容**（v0.6 → v0.7，657 → 846 行）：
- 架构图重绘：补人员中台 8001 + 5 端口拓扑（3306/8000/8001/5173/3210）+ 看门狗
- 新增**模块10 任务中心**（6 类待办聚合 + dateflags 单一判据铁律）、**模块11 人员中台**（独立服务 + pmwb_org/pmwb_staff 建表 + 禁自建人员表铁律）
- 模块6 知识库 → 知识中心四子页（补 pmwb_sql_script）；模块8 补邮件中心 5 子页 + 降级契约；模块5 补 pmwb_meeting_agenda；模块2 补 pmwb_user_story；模块3 补 result_feedback/attachments；模块0 记 DashboardV2 废弃决策 + 禁用 cast(col,Date) 口径
- 8.2 表总览 22 → 28 张

**其他文档**：开发工单模块设计补两列；architecture_review 标历史快照（列已落地整改项）；ui-redesign-proposals 标提案（仅局部采纳）。

**README 纠错**：昨天写的「27 张表」「20 个迁移」是错的，实际 28 张 / 23 个；新增「文档索引」章节（7 份文档标注现行/快照/归档）。

**提交**：`faa46ea`（分支 feature/docs-0804-review，与 c2e8314 共 2 个提交待推送）。

**约定**：文档数量类表述（表数/迁移数/路由数）必须现场 grep 实测，不得凭记忆写——本次两处都错了。

## [收尾] 文档分支推送（老大指令：推送）

- 安全闸门：`git merge-base origin/main HEAD` = be5c653（非孤儿分支，可 fast-forward）。
- 遗留 `.workbuddy/memory/2026-08-04.md`（18 行文档校准留痕）先提交为 `784cf71`，再推送。
- `git push origin feature/docs-0804-review`（origin 新增该分支留档）→ `git checkout main` + `git merge --ff-only` → `git push origin main`：be5c653..784cf71。
- 验证 `origin/main...main = 0 0`，三提交（c2e8314 / faa46ea / 784cf71）已入 main 并推 origin。
- 现状：main 干净，文档校准成果全部落库；每日扫描🔴通道持续静默。

## [修复] 任务中心邮件标题/正文截断丢失（老大指令：继续优化邮件通知/督办）

**现象**：邮件预览页面中工单标题超长时后半段丢失；实际发送亦可能截断/落库失败。

**根因（端到端 5 处截断）**：
1. 前端 `TaskCenterView.vue:555` 主题预填 `first.title?.slice(0,40)`；
2. 后端 `services/task_center.py` 各 collect_* 对 content/situation_desc/risk_note/note/description 施 `[:80]/[:100]/[:200]/[:300]`；
3. `build_email_body` 对补充说明 `str(desc)[:500]`；
4. 模型 `EmailRecord.subject` 为 `String(500)`，长标题落库截断/报错；
5. 发送路径无截断（EmailCenterClient 透传全量，已确认）。

**修复（分支 feature/tc-email-full-content，提交 56e85af）**：
- 前端去 `slice(0,40)`，主题用完整 `(first.title || '')`；
- 后端移除全部内容截断点（保留 req_id[:64]/req_name[:255] 系 DB 列元数据上限，非正文）；
- 模型 `subject` String(500)→Text；content 本就 Text；
- 迁移 `20260804000002_email_record_subject_text.py`（**原拟 20260804000001，与既有 add_minutes_required 修订号冲突已重排**）。

**验证**：`alembic upgrade head` 成功，`email_records.subject` 实测为 `text` 类型；pytest(task_center) 13 passed，全量 116 passed/3 failed(环境依赖,无回归)；vite build 干净。
**状态**：已提交 feature 分支，领先 origin/main 1 提交，**未推送**（按铁律只提醒不自动推，待老大确认合版+推送）。
