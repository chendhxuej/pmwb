# 交接单模板 → 复制为 handoff/<branch>.md 填写

- **分支**：feature/xxx
- **任务号**：<模块>-<序号>
- **负责人 AI**：<名称>
- **改动文件清单（绝对路径）**：
  - 
- **接线点（建表 / ALTER / main.py 注册 / router 菜单等）**：
  - 
- **自测结果（必须含真实访问验证：HTTP 状态码 / 页面渲染 / 单测）**：
  - 
- **可否入库**：是 / 否
- **备注**：

> 填完后把任务号追加进 `../INTEGRATION_QUEUE.md` 的「待处理」区。
