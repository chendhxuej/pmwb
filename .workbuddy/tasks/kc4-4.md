# kc4-4 需求交付物去开发工单

- 分支：认领后建子分支 `feature/kc4-4-delivery`，PR 合入 `feature/kc4-knowledge-hub`（Vicky2号 审查合版）
- 级别：S2
- 参考：`docs/kc3_knowledge_hub_plan.md` §3.2 / §4.4 / §5(kc4-4)

## 目标
取消「开发工单」中间实体：需求交付物（操作手册等文件）改为需求直接挂载，经 archive 归档到 Obsidian `06-交付物/` 并回写主笔记 §6。

## 边界
- 开发工单（`pmwb_dev_ticket` / `sediment_dev_ticket` / `archive_requirement_manual` 原依赖）退出链路，数据保留不删；相关路由标记 deprecated。
- 需求交付物：`PmwbRequirementExt` 增 `deliverables`（JSON 或新建关联表 `pmwb_requirement_delivery`），存文件路径 / 备注。

## 落点文件
- `backend/db/models.py`：`PmwbRequirementExt` 增 `deliverables` 字段（或新表）。
- `backend/services/obsidian_link.py`：`archive_requirement_manual` 改为读需求直挂文件 → 写 `06-交付物/{文件名}` → 建 link(`event_type=delivery`) → 触发 `sync`（kc4-2）。
- `backend/routers/requirement.py`：新增交付物上传 / 列举端点；前端需求详情接入。

## 验收
- 需求直挂文件 → 归档 06 + 主笔记 §6 自动出现该交付物。
- 旧开发工单数据仍在，不报错。

## 依赖
- blockedBy **kc4-2**（归档后需回流主笔记 §6）。
