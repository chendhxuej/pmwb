# 产品经理个人工作台 — 需求规格说明书

> 系统标识：`pmwb_`  
> 邮件模板类型前缀：`pmwb_{业务类型}`  
> 文档版本：v0.7（2026-08-04 对照实际代码全面校准）

---

## 一、项目概述

### 1.1 项目背景

本人作为中国移动通信集团江苏有限公司数字化部集客业务产品经理，日常核心工作涵盖：集客业务销售能力建设、生产数据运营保障、需求管理、投诉处理、会议协调及知识沉淀等多个条线。虽然已有公司级标准化系统（需求管理系统、投诉支撑系统、OA系统等），但缺乏一个以个人工作流为核心、跨系统聚合信息、并能主动沉淀业务知识的统一管理平台。

目前已具备以下基础能力：
- **需求催办系统**（`D:\项目\需求催办系统`）— Node.js 后端 + MySQL，已运行，6000+条需求数据
- **统一邮件中心**（`D:\项目\统一邮件中心`）— HTTP API 邮件发送服务，端口 3210
- **Obsidian 知识库**（`D:\项目\知识图谱`）— 本地 Markdown 知识管理工具，600+需求卡片，21类家客产品知识
- **Chrome 浏览器插件** — 需求信息提取 + 催办邮件发送

> **架构整合说明（2026-07-16）**：上述分散系统已于 2026-07-16 全部并入 PMWB 统一入口，不再作为独立系统运行——需求催办系统的网页功能（催办中心/邮件记录/集团需求）并进 PMWB 前端；Chrome 插件改连 PMWB 后端（`/api/v1/plugins/*`），原 2525 本地中继退役；3000 端口服务可停。详见「模块8：统一邮件催办」。

### 1.2 建设目标

1. **一个入口，全面管理**：通过本地浏览器访问的 Web 系统，统一管理需求、运营、会议、待办、知识等全部工作内容。
2. **数据驱动，主动可视**：关键指标、任务进度、风险预警在首页看板一目了然，从“被动响应”转向“主动管控”。
3. **流程闭环，知识沉淀**：需求从提出到交付、运营从发现问题到解决问题、会议从计划到决议执行，全流程留痕，并自动沉淀为结构化知识资产。
4. **易于扩展，面向 AI**：积累的业务数据与知识可供未来 AI 助手分析利用。

### 1.3 系统名称与标识

| 维度 | 命名 | 说明 |
| :--- | :--- | :--- |
| 系统中文名 | **产品经理个人工作台** | 正式名称 |
| 系统英文标识 | **`pmwb`** | 数据库表前缀、代码命名空间 |
| 邮件模板类型 | **`pmwb_{业务类型}`** | 如 `pmwb_reminder`、`pmwb_meeting` |

### 1.4 目标用户

| 用户角色 | 使用场景 | 核心诉求 |
| :--- | :--- | :--- |
| 产品经理（本人） | 日常工作的统一入口 | 掌控全局、减少遗漏、快速沉淀 |
| 外部协作者（可选） | 共享进度、查看报告 | 只读或有限协作，非本阶段重点 |

### 1.5 使用场景

- **每日上班**：打开首页看板，查看今日待办、超期事项、今日会议。
- **需求跟踪**：查看需求列表，点击需求查看关联开发工单、邮件催办记录、交付物归档。
- **运营问题处理**：录入或跟踪生产运营问题，记录根因与解决方案，沉淀为 Bug 解决方案或运维 SOP。
- **会议后**：新建会议纪要，生成待办并关联到责任人和相关需求/工单。
- **周/月复盘**：通过统计报表查看需求处理量、工单上线情况、运营问题闭环率。

---

## 二、系统架构

### 2.1 总体架构

```
+------------------+      +-------------------+      +-----------------+
|   Chrome 插件     |      |   Python 定时脚本  |      |   Obsidian Vault |
| (需求采集+         | ---> | (采集/清洗/聚合)    |      | (Markdown知识库) |
|  开发工单采集)      |      |                   |      +--------+--------+
+------------------+      +-------------------+               |
        |                         |                           |
        v                         v                           v
+---------------------------------------------------------------+
|         产品经理个人工作台 主后端 (FastAPI, localhost:8000)        |
|              RESTful API + Markdown 读写 + 服务编排               |
+--+---------------+------------------+-------------------------+-+
   |               |                  |                         |
   v               v                  v                         v
+----------+  +-------------+  +----------------+  +----------------------+
| MySQL    |  | 人员中台     |  | 统一邮件中心    |  | Obsidian Vault        |
| yxtyg_db |  | Master:8001 |  | localhost:3210 |  | D:\项目\知识图谱       |
| sent_    |  | 组织/人员    |  | POST /api/send |  | Markdown 读写          |
| emails + |  | 唯一数据源   |  | /api/templates |  |                      |
| pmwb_*   |  | (独立 FastAPI|  |  /:id/render   |  |                      |
+----------+  |  +alembic)  |  +----------------+  +----------------------+
              +-------------+
                     ^
                     |  前端 StaffSelect 组件 / 后端 master_service.py 代理
+---------------------------------------------------------------+
|          前端 Vue3 + Element Plus (Vite dev, localhost:5173)     |
+---------------------------------------------------------------+
```

> **服务拓扑（2026-08-04 实测）**：共 5 个端口——MySQL `3306` → 主后端 `8000` / 人员中台 `8001` → 前端 `5173`，外加统一邮件中心 `3210`（独立项目）。本地由看门狗脚本 `pmwb-keeper.py` 常驻保活，详见 README「启动方式」。

### 2.2 技术选型

| 层次 | 技术 | 说明 |
| :--- | :--- | :--- |
| 后端框架 | FastAPI | 现代异步 Python Web 框架，自动生成 OpenAPI 文档 |
| 前端框架 | Vue 3 + Element Plus | 组件丰富，适合管理后台类系统 |
| 数据库 | MySQL（yxtyg_db） | 复用现有库，新增 `pmwb_*` 表；迁移用 Alembic |
| 文件/知识库 | Obsidian Vault | 本地 Markdown 存储，通过后端读写 |
| 邮件服务 | 统一邮件中心 | HTTP API 调用，不直接连 SMTP |
| 人员/组织 | 人员中台 Master Service（8001） | 独立 FastAPI 服务 + 独立 alembic，全站人员唯一数据源 |
| 定时任务 | APScheduler / 系统任务计划 | 用于数据采集、提醒、聚合 |
| 部署方式 | 本地运行 + 看门狗保活 | 个人使用，localhost 访问；`pmwb-keeper.py` 每 15s 巡检 5 端口 |
| 测试 | pytest（后端）/ Vitest（前端） | 基线：pytest 116 passed、vitest 7/7、vite build 干净 |

### 2.3 邮件服务

所有邮件发送统一通过统一邮件中心 HTTP API，不使用直接 SMTP 连接。

| 接口 | 方法 | 说明 |
| :--- | :--- | :--- |
| `/api/send` | POST | 发送邮件 |
| `/api/templates/:id/render` | POST | 渲染邮件模板 |

模板命名规范：`pmwb_{业务类型}`，如：
- `pmwb_reminder` — 需求催办
- `pmwb_meeting_invite` — 会议邀请
- `pmwb_meeting_minutes` — 会议纪要分发
- `pmwb_deliverable_notice` — 交付物通知

---

## 三、模块规划

### 模块0：首页看板

**定位**：系统首页，聚合所有关键信息，提供“今日工作”总览。

**Obsidian 联动**：保留 `00-工作台/工作台看板.md` 作为离线备用版。

**数据来源**：MySQL 各表聚合（待办数/超期数/需求各状态统计/工单进度/会议日程）。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 今日待办 | 展示今日到期或优先级高的待办，支持快捷完成 |
| 超期预警 | 红色高亮显示已逾期的待办、工单、运营问题 |
| 今日会议 | 展示今日会议列表，快速进入纪要 |
| 关键指标卡 | 需求总数、在途工单数、未闭环运营问题数、本周会议数 |
| 需求状态分布 | 饼图/柱状图展示需求各状态占比 |
| 工单进度跟踪 | 最近活跃的开发工单列表及进度 |
| 快捷入口 | 新建待办、新建工单、新建会议纪要、发送催办 |
| 通知中心 | 系统提醒、邮件发送结果、定时任务状态 |
| 模块卡片 | 需求与交付 / 运营监控 / 会议日程 / 重点工作 / 任务中心 5 类模块统计卡（2026-08-03 补齐） |

> **实现说明（2026-08-03 决策）**：曾并行开发过新版看板 `DashboardV2View.vue`（`/dashboard-v2`），已由老大决定**废弃并删除**，回归旧版 `HomeView.vue` 作为唯一看板页直接迭代。后端聚合接口 `/api/v1/dashboard/*` 只服务此页。
>
> ⚠️ 统计口径约定：会议统计走 naive datetime 区间比较，**不得使用 `cast(start_time, Date)`**——该写法在 SQLite（单测库）走 numeric affinity 直接失效；催办/逾期判据统一走 `backend/utils/dateflags.py`，禁止各处自行实现。

### 模块1：待办中心

**定位**：管理个人所有待办任务，支持与需求、工单、会议、运营问题关联。

> 本模块为**个人待办**单一数据源（菜单「个人待办」`/todo`）。跨模块的统一待办视图另见 **模块10：任务中心**。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 待办创建 | 标题、内容、分类、优先级、截止日期、重复周期、提醒时间 |
| 待办分类 | 工作分类：需求跟进、工单跟踪、运营问题、会议准备、学习、其他 |
| 优先级 | P0 / P1 / P2 / P3，P0 默认置顶并红色标记 |
| 状态管理 | 待处理 / 进行中 / 已完成 / 已取消 |
| 业务关联 | 可关联到需求、开发工单、运营问题、会议 |
| 重复任务 | 支持按日/周/月重复，如每周运营周报 |
| 提醒通知 | 到期前提醒，首页看板超期高亮 |
| 视图切换 | 列表视图、看板视图（按状态）、日历视图 |
| 筛选排序 | 按分类、优先级、状态、截止日期筛选排序 |

**数据表**：`pmwb_todo`

```sql
CREATE TABLE pmwb_todo (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '自增ID',
    title VARCHAR(255) NOT NULL COMMENT '待办标题',
    content TEXT COMMENT '待办内容',
    category VARCHAR(64) DEFAULT 'other' COMMENT '分类：requirement/ticket/operation/meeting/study/other',
    priority ENUM('P0','P1','P2','P3') DEFAULT 'P2' COMMENT '优先级',
    status ENUM('todo','in_progress','done','cancelled') DEFAULT 'todo' COMMENT '状态',
    due_date DATE COMMENT '截止日期',
    due_time TIME COMMENT '截止时间（可选）',
    remind_at DATETIME COMMENT '提醒时间',
    repeat_type ENUM('none','daily','weekly','monthly') DEFAULT 'none' COMMENT '重复类型',
    related_type VARCHAR(64) COMMENT '关联对象类型：requirement/ticket/operation/meeting',
    related_id VARCHAR(64) COMMENT '关联对象ID',
    source VARCHAR(64) COMMENT '来源：manual/meeting/plugin',
    completed_at DATETIME COMMENT '完成时间',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_status (status),
    INDEX idx_due_date (due_date),
    INDEX idx_related (related_type, related_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='待办任务表';
```

### 模块2：需求管理

**定位**：在现有 `sent_emails` 表基础上，做需求信息的集中展示、增强、跟踪与统计。

**现有数据**：`sent_emails` 表 6000+条需求记录。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 需求列表 | 展示所有需求，支持分页、搜索、筛选、排序 |
| 需求详情 | 点击需求查看完整信息、催办历史、关联工单、交付物、会议纪要 |
| 状态跟踪 | 需求状态：已提出 / 已受理 / 开发中 / 已上线 / 已关闭 / 已暂停 |
| 催办记录 | 展示该需求历次催办邮件发送记录（来自 `email_records`） |
| 关联工单 | 展示该需求关联的所有开发工单 |
| 标签/备注 | 为需求添加个人标签、内部备注 |
| 快速催办 | 在需求详情页一键发送催办邮件 |
| 需求统计 | 按状态、按系统、按月份统计需求数量 |
| 版本要求 | 为需求设置「版本要求」日期（需求管理要求的上线时间），页面手选；作为关联开发工单进度跟踪的基准 |
| 跟踪状态 | 基于版本要求日期与关联工单进度自动计算三级预警：按时(绿)/进行中(蓝)/预警(黄·距版本≤7天)/超期(红·未上线已过版本或上线晚于版本)/无工单(灰) |
| 团队评估记录 | 展开行只读展示团队评估（评估SA/系统/工作量/复核工作量/开发单号/意见），支持「编辑」弹窗修改 |
| 关联工单进度 | 展开行展示该需求全部关联开发工单（工单号/系统/状态/进度%/开发完成/测试完成/上线日），超期工单红色高亮 |
| 列表交互 | 需求名称点击直达详情；点击表格任意行展开/收起团队评估与工单进度面板 |
| 用户故事 | 按需求维护用户故事（标题/描述/场景/验收标准/业务规则），支持草稿与定稿两态 |

**数据表**：复用 `sent_emails` 作为主数据源，新增扩展表 `pmwb_requirement_ext` 存储个人管理字段；另有 `pmwb_requirement_evaluation`（团队评估，key=sent_email_id）与 `pmwb_user_story`（用户故事）。

```sql
CREATE TABLE pmwb_user_story (
    id INT AUTO_INCREMENT PRIMARY KEY,
    req_id VARCHAR(64) NOT NULL COMMENT '需求编号',
    seq INT DEFAULT 1 COMMENT '故事序号',
    title VARCHAR(500) COMMENT '故事标题',
    `desc` TEXT COMMENT '故事描述',
    scene TEXT COMMENT '故事场景',
    acceptance TEXT COMMENT '验收标准(JSON数组)',
    rules TEXT COMMENT '业务规则(JSON数组)',
    finalized INT DEFAULT 0 COMMENT '是否已定稿 0草稿/1定稿',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_story_req_seq (req_id, seq),
    INDEX idx_user_story_req_id (req_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='需求用户故事表';
```

```sql
CREATE TABLE pmwb_requirement_ext (
    id INT AUTO_INCREMENT PRIMARY KEY,
    req_id VARCHAR(64) NOT NULL COMMENT '需求编号，对应 sent_emails.req_id',
    status VARCHAR(64) DEFAULT 'proposed' COMMENT '个人跟踪状态：proposed/accepted/dev/closed/paused',
    tags VARCHAR(512) COMMENT '个人标签，逗号分隔',
    personal_note TEXT COMMENT '个人备注',
    priority ENUM('P0','P1','P2','P3') DEFAULT 'P2' COMMENT '个人优先级',
    version_required_date DATE COMMENT '版本要求(需求管理要求的上线时间)，作为工单进度跟踪基准',
    owner_note TEXT COMMENT '负责人备忘',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_req_id (req_id),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='需求管理扩展表';
```

### 模块3：开发工单跟踪（已确认）

**定位**：跟踪每个需求在各系统中的开发工单全生命周期。

**状态流**：

```
开发单已创建 → 设计方案已评审 → 开发已完成 → 测试已完成 → 已上线 → 已归档
```

**详细设计**：详见《开发工单模块设计.md》。

**核心表**：`pmwb_dev_ticket`、`pmwb_dev_deliverable`、`pmwb_dev_ticket_log`。

**交付物归档**：`02-需求资产/交付物/{req_id}/{ticket_no}/`，含自动生成的 `交付物清单.md`。

**增量能力（2026-08-01/08-04）**：工单支持**处理结果反馈**与**附件上传**——`pmwb_dev_ticket` 新增 `result_feedback`（处理结果反馈文本）、`attachments`（附件元信息 JSON 数组）两列（迁移 `20260801000001`）；录入弹窗接入抽屉草稿持久化（`useDrawerDraft`），意外关闭不丢内容。

### 模块4：业务运营监控

**定位**：跟踪生产运营问题、数据异常、一线投诉，形成“发现 → 处理 → 复盘 → 沉淀”闭环。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 问题录入 | 问题标题、类型、发现时间、影响范围、关联系统、关联需求/工单 |
| 问题类型 | 数据异常 / 系统故障 / 一线投诉 / 流程阻塞 / 性能问题 / 其他 |
| 状态跟踪 | 待处理 / 处理中 / 待验证 / 已解决 / 已关闭 / 已挂起 |
| 处理记录 | 记录每次排查过程、根因分析、解决方案 |
| 影响评估 | 影响客户数、影响业务量、是否需上报 |
| 关联沉淀 | 解决问题后，可一键生成 Bug 解决方案或运维 SOP 知识条目 |
| 统计报表 | 按问题类型、按系统、按月份统计问题数量与闭环率 |
| 重点关注 | 超期未解决、影响重大的问题在首页高亮 |

> **分类说明（2026-07-16 重构 + 07-16 录单优化）**：运营监控采用「1 张主表 + 5 大类」结构。`pmwb_operation_issue` 用 `category` 字段区分 5 大类——`bug`(BUG管理) / `data`(数据异常管理) / `prod`(生产问题分析) / `task`(临时交办任务) / `complaint`(热点投诉)。`issue_type` 改为更通用的 6 种子类：`bug`/`data_abnormal`/`topic_analysis`/`spot_event`/`temp_task`/`other`。`handler` 支持多选，存储为逗号分隔字符串（VARCHAR 512）。原字段 `impact_scope` 已改名为 `situation_desc`（情况说明）。每类页顶部含「闭环率」= (已解决+已关闭)/总数，并支持关联 Obsidian 知识笔记，实现数据库+笔记联动管理。

**数据表**：`pmwb_operation_issue`

```sql
CREATE TABLE pmwb_operation_issue (
    id INT AUTO_INCREMENT PRIMARY KEY,
    issue_no VARCHAR(64) NOT NULL COMMENT '问题编号',
    title VARCHAR(255) NOT NULL COMMENT '问题标题',
    category ENUM('bug','data','prod','task','complaint') DEFAULT 'prod' COMMENT '工单大类',
    issue_type ENUM('bug','data_abnormal','topic_analysis','spot_event','temp_task','other') DEFAULT 'other' COMMENT '问题子类',
    status ENUM('pending','processing','verify','resolved','closed','suspended') DEFAULT 'pending' COMMENT '状态',
    source VARCHAR(64) COMMENT '来源：monitor/complaint/oa/manual',
    discovery_date DATETIME COMMENT '发现时间',
    resolve_date DATETIME COMMENT '解决时间',
    handler VARCHAR(512) COMMENT '处理人(多选,逗号分隔)',
    situation_desc TEXT COMMENT '情况说明',
    impact_level ENUM('P0','P1','P2','P3') DEFAULT 'P2' COMMENT '影响等级',
    root_cause TEXT COMMENT '根因分析',
    solution TEXT COMMENT '解决方案',
    related_req_id VARCHAR(64) COMMENT '关联需求编号',
    related_ticket_no VARCHAR(64) COMMENT '关联开发工单编号',
    related_system VARCHAR(128) COMMENT '关联系统',
    obsidian_path VARCHAR(512) COMMENT '沉淀知识条目路径',
    is_overdue TINYINT(1) DEFAULT 0 COMMENT '是否超期',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_issue_no (issue_no),
    INDEX idx_status (status),
    INDEX idx_type (issue_type),
    INDEX idx_category (category),
    INDEX idx_related_req (related_req_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='业务运营问题表';
```

### 模块5：会议管理

**定位**：管理会议计划、会议纪要、会议决议与后续行动项。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 会议创建 | 会议主题、时间、地点、参会人、会议类型、关联需求/工单 |
| 参会人管理 | 从联系人库选择，支持手动添加 |
| 会议纪要 | 按模板录入会议纪要，包含议题、结论、行动项 |
| 行动项生成 | 从会议纪要中生成待办，自动关联责任人 |
| 会议分类 | 需求评审 / 项目例会 / 问题排查 / 培训 / 其他 |
| 会议归档 | 会议纪要按 `03-会议资产/{年份}/{meeting_id}.md` 归档到 Obsidian |
| 会议列表 | 按时间、类型、状态筛选，查看历史会议 |
| 会议提醒 | 会议开始前提醒 |
| 议题管理 | 按议题项维护：议题标题、**背景说明**、商讨结论、分工说明，可排序（2026-08-01/08-04 新增） |
| 行动项独立页 | 「会议日程 → 行动项」子页统一查看/督办全部会议行动项 |
| 纪要邮件 | 按议题项生成纪要邮件；收件人支持「中文姓名 或 邮箱」混合输入，姓名经人员中台解析 |

**数据表**：`pmwb_meeting`、`pmwb_meeting_agenda`、`pmwb_meeting_attendee`、`pmwb_meeting_action`

```sql
CREATE TABLE pmwb_meeting (
    id INT AUTO_INCREMENT PRIMARY KEY,
    meeting_id VARCHAR(64) NOT NULL COMMENT '会议编号',
    title VARCHAR(255) NOT NULL COMMENT '会议主题',
    meeting_type ENUM('requirement_review','project_weekly','troubleshooting','training','other') DEFAULT 'other' COMMENT '会议类型',
    start_time DATETIME COMMENT '开始时间',
    end_time DATETIME COMMENT '结束时间',
    location VARCHAR(255) COMMENT '会议地点/线上链接',
    host VARCHAR(64) COMMENT '主持人',
    summary TEXT COMMENT '会议纪要摘要',
    obsidian_path VARCHAR(512) COMMENT 'Obsidian 纪要路径',
    related_req_id VARCHAR(64) COMMENT '关联需求编号',
    related_ticket_no VARCHAR(64) COMMENT '关联开发工单编号',
    status ENUM('planned','held','cancelled') DEFAULT 'planned' COMMENT '状态',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_meeting_id (meeting_id),
    INDEX idx_start_time (start_time),
    INDEX idx_related_req (related_req_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会议表';

CREATE TABLE pmwb_meeting_agenda (
    id INT AUTO_INCREMENT PRIMARY KEY,
    meeting_id INT NOT NULL COMMENT '关联 pmwb_meeting.id',
    seq INT DEFAULT 1 COMMENT '议题序号（用于排序）',
    topic VARCHAR(255) NOT NULL COMMENT '议题标题',
    background TEXT COMMENT '议题背景说明',
    conclusion TEXT COMMENT '商讨结论',
    division TEXT COMMENT '分工说明',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_agenda_meeting_id (meeting_id),
    FOREIGN KEY (meeting_id) REFERENCES pmwb_meeting(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会议议题表';

CREATE TABLE pmwb_meeting_attendee (
    id INT AUTO_INCREMENT PRIMARY KEY,
    meeting_id INT NOT NULL COMMENT '关联 pmwb_meeting.id',
    name VARCHAR(64) NOT NULL COMMENT '参会人姓名',
    email VARCHAR(128) COMMENT '邮箱',
    dept VARCHAR(128) COMMENT '部门',
    is_required TINYINT(1) DEFAULT 1 COMMENT '是否必须参加',
    INDEX idx_meeting_id (meeting_id),
    FOREIGN KEY (meeting_id) REFERENCES pmwb_meeting(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会议参会人表';

CREATE TABLE pmwb_meeting_action (
    id INT AUTO_INCREMENT PRIMARY KEY,
    meeting_id INT NOT NULL COMMENT '关联 pmwb_meeting.id',
    content TEXT NOT NULL COMMENT '行动项内容',
    owner VARCHAR(64) COMMENT '负责人',
    due_date DATE COMMENT '截止日期',
    status ENUM('pending','done','cancelled') DEFAULT 'pending' COMMENT '状态',
    related_todo_id INT COMMENT '关联 pmwb_todo.id',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_meeting_id (meeting_id),
    FOREIGN KEY (meeting_id) REFERENCES pmwb_meeting(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='会议行动项表';
```

### 模块6：知识中心（原「知识库」，2026-07-29 起为四子页聚合）

**菜单结构**：`知识中心` 一级菜单下含 4 个子页——

| 子页 | 路由 | 说明 |
| :--- | :--- | :--- |
| 知识库 | `/knowledge-center/knowledge` | 本节所述 Obsidian 联动知识条目 |
| 产品圣经 | `/knowledge-center/product-bible` | 见模块7 |
| 知识沉淀 | `/knowledge-center/notes` | 随手笔记与经验沉淀入口 |
| SQL脚本库 | `/knowledge-center/sql-scripts` | 常用取数/核查 SQL 归档复用 |

**SQL 脚本库数据表**：`pmwb_sql_script`

```sql
CREATE TABLE pmwb_sql_script (
    id INT AUTO_INCREMENT PRIMARY KEY,
    script_no VARCHAR(64) NOT NULL UNIQUE COMMENT '脚本编号 SQL-YYYYMMDD-XXX',
    title VARCHAR(500) NOT NULL COMMENT '脚本说明/名称',
    category VARCHAR(64) COMMENT '业务线/分类（可空，允许自定义）',
    description TEXT COMMENT '补充说明',
    sql_text TEXT NOT NULL COMMENT 'SQL 文本',
    output_fields TEXT COMMENT '输出字段样例(JSON数组: name/type/desc)',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_sql_category (category)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='SQL脚本库表';
```

#### 6.1 知识库（Obsidian 联动）

**定位**：将系统内沉淀的需求、工单、运营问题、会议等资产，与 Obsidian 知识库打通，形成可检索、可复用的知识条目。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 知识条目列表 | 展示知识库中所有条目，按分类、标签筛选 |
| 知识分类 | 产品档案 / 运营知识 / 需求资产 / 会议资产 / 个人知识 / 学习知识 |
| 标签体系 | 统一标签管理，支持多标签 |
| 全文检索 | 搜索知识条目标题与内容 |
| 来源关联 | 知识条目可关联到需求、工单、运营问题、会议 |
| 模板创建 | 按模板新建产品档案、运维 SOP、Bug 解决方案等 |
| 快速查看 | 在工作台内直接预览 Markdown 内容 |
| 目录同步 | 按 Obsidian 目录结构读写，保持两边一致 |

**数据表**：`pmwb_knowledge_item`

```sql
CREATE TABLE pmwb_knowledge_item (
    id INT AUTO_INCREMENT PRIMARY KEY,
    item_id VARCHAR(64) NOT NULL COMMENT '知识条目编号',
    title VARCHAR(255) NOT NULL COMMENT '标题',
    category VARCHAR(64) NOT NULL COMMENT '分类：product/operation/requirement/meeting/personal/study',
    sub_category VARCHAR(128) COMMENT '子分类',
    tags VARCHAR(512) COMMENT '标签，逗号分隔',
    obsidian_path VARCHAR(512) NOT NULL COMMENT 'Obsidian 文件路径',
    source_type VARCHAR(64) COMMENT '来源类型：requirement/ticket/operation/meeting/manual',
    source_id VARCHAR(64) COMMENT '来源对象ID',
    summary TEXT COMMENT '摘要',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_item_id (item_id),
    INDEX idx_category (category),
    INDEX idx_source (source_type, source_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='知识库条目表';
```

### 模块7：产品圣经（ProductBible）

**定位**：内置产品档案/运维知识检索与编辑入口，实时读取 Obsidian 知识库作为单一事实源，不落库。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 业务切换 | 顶部业务切换器，按 `PRODUCT_BIBLE` 配置加载业务（当前已接入「集团短信业务」，新增业务仅加一行配置） |
| 文档阅读 | 左侧 TOC + 正文渲染；\`\`\`mermaid\`\`\` 自动成图；宽表横向滚动；**H2 章节可折叠/展开其下 H3 子章节** |
| 关键字查询高亮 | 左侧搜索框输入关键词，正文命中处黄色高亮 + 计数(1/N) + ↑↓ 翻页 + × 清除 |
| 在线编辑写回 | 「编辑内容」进入 Markdown 源码框，保存经确认后写回 Obsidian vault 对应 `.md`（后端 `PUT /api/v1/product-bible/{key}`） |

**数据来源**：Obsidian Vault（`D:\项目\知识图谱`），业务域→路径映射在 `backend/core/config.py` 的 `PRODUCT_BIBLE` 列表。无新增数据库表。

### 模块8：统一邮件催办

**定位**：吸收原「需求催办系统」全域——按 SA 分组待催办 + 批量催办、全局邮件记录、集团需求独立页与导出、邮件中心健康检测、待办「开发单号未录入」卡、首页「邮件中心」状态卡。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 催办中心 | 按 SA 分组待催办需求，批量发送催办邮件（收件人经统一邮件中心按姓名解析） |
| 邮件记录 | 全局催办邮件发送记录查询与详情 |
| 集团需求 | 集团需求独立列表 + 导出 xlsx |
| 邮件中心健康 | 检测 3210 邮件中心连通（database/smtp 状态） |
| 待办联动 | 「开发单号未录入」待办卡、首页邮件中心状态卡 |

**后端接口**：`/api/v1/reminders/pending`、`/api/v1/reminders/records`、`/api/v1/mail-center/health`、`/api/v1/requirements`（含 `dev_ticket_missing` 统计）；插件接入 `/api/v1/plugins/*`（health/send/ingest/contacts）。
**数据表**：复用 `sent_emails` / `email_records` / `sa_info`（统一在 yxtyg_db）；新增 `pmwb_requirement_evaluation`（需求团队评估，key=sent_email_id）。

#### 8.1 邮件中心（2026-07-28 整合，独立一级菜单）

原「统一邮件中心」（3210）的管理界面已整体并入 PMWB，成为 `邮件中心` 一级菜单，含 5 个子页；后端经 `/api/v1/mail-center/*` 代理，不直连 SMTP。

| 子页 | 路由 | 说明 |
| :--- | :--- | :--- |
| 发送日志 | `/mail-center/logs` | 全量邮件发送记录查询，含统计概览（发送量/成功率趋势） |
| 邮件账号 | `/mail-center/accounts` | 发信账号配置与状态 |
| 通讯录 | `/mail-center/contacts` | 联系人管理（收件人统一走人员中台解析） |
| 联系人分组 | `/mail-center/groups` | 分组维护，支持按组群发 |
| 邮件模板 | `/mail-center/templates` | `pmwb_*` 模板维护与渲染预览 |

> **降级契约**：`EmailCenterClient.send_email(...)` 返回 `{"ok", "data"?, "error"?}`；业务侧用 `raise_on_error=False` 判 `result["ok"]`，发送失败只落 `send_status=failed` + 记 `error_msg`，**不得抛异常中断接口**。

### 模块9：重点工作（2026-07-21 新增）

**定位**：面向「总部试点 / 年度任务 / 专题工作」三类重点工作的全过程管理。三类共用同一套数据结构，仅靠 `category` 字段区分；前端单一「重点工作」菜单内部以 Tab 切换分类。交付物统一归档至 Obsidian Vault 的 `09-重点工作/{category}/{工作编号}_{标题}` 目录，实现工作过程与知识资产联动。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 分类视图 | 全部 / 总部试点 / 年度任务 / 专题工作 Tab 切换；KPI 概览（按状态计数、超期待办、即将到期里程碑） |
| 基本信息 | 工作背景、现状说明、工作内容、创建时间、计划完成时间、优先级、状态 |
| 工作目标 | 多目标条目（内容 + 指标），可增删改 |
| 验收标准 | 多条验收标准（列表形式），落库为 JSON、前端按数组展示 |
| 任务里程碑 | 里程碑名称、计划完成日期、状态（待启动/进行中/已完成/延期） |
| 团队分工 | 成员姓名、角色、分工说明 |
| 月度计划 | 按月份记录月度工作计划 |
| 周计划 | 按周次（ISO 周，如 2026-W32）记录周计划 |
| 工作进展 | 进展记录（内容 + 记录时间 + 进展百分比），支持多条追加 |
| 成员待办 | 模块内专属待办（区别于全局待办中心），含状态与关联链接，支持部分更新（仅传 status 即可） |
| 交付物管理 | 文件上传/列表/下载/删除，落盘至 Obsidian `09-重点工作` 对应目录，路径穿越防护 |

**后端接口**：`/api/v1/key-works`（列表/统计/详情/主 CRUD）；子表 `POST/PUT/DELETE /{kw_id}/progress`、`/member-tasks`、`/milestones`、`/members`、`/monthly-plans`、`/weekly-plans`；交付物 `POST /{kw_id}/deliverables/upload`、`GET /{kw_id}/deliverables`、`GET /{kw_id}/deliverables/{did}/download`、`DELETE /{kw_id}/deliverables/{did}`。
**数据表**：新增 9 张 `pmwb_key_work*`（主表 `pmwb_key_work` + 8 子表：goals / milestones / members / monthly_plans / weekly_plans / progresses / member_tasks / deliverables），统一在 yxtyg_db，详见 8.2。

### 模块10：任务中心（2026-07-29 新增）

**定位**：跨模块的**统一待办视图**，把散落在 6 个模块里的待办聚合到一屏，解决"多处找事做"的问题。位于首页看板之后的第二个一级菜单（`/task-center`），是日常工作的主要入口。

**聚合来源**（`backend/schemas/task_center.py: TASK_SOURCES`）：

| source | 展示名 | 底层数据 |
| :--- | :--- | :--- |
| `todo` | 个人待办 | `pmwb_todo` |
| `operation_issue` | 运营问题 | `pmwb_operation_issue` |
| `dev_ticket` | 开发工单 | `pmwb_dev_ticket` |
| `meeting_action` | 会议行动项 | `pmwb_meeting_action` |
| `key_work` | 重点工作 | `pmwb_key_work` 成员任务 / 里程碑 / 主项 |
| `requirement_urge` | 需求催办 | `sent_emails` 派生，判据以 `pmwb_requirement_evaluation` 为准 |

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 统一状态 | 各源状态归一为 `pending / in_progress / done / blocked` 四态 |
| 来源筛选 | 按 6 类来源、状态、逾期标记筛选；每项可跳回原模块（`source_url`） |
| 逾期标记 | 统一走 `utils/dateflags.py` 的 `flag_due_date`，与首页看板口径一致 |
| 统计概览 | 按来源/状态计数，超期与临期分色 |
| 邮件催办 | 选中任务直接发催办邮件（经统一邮件中心） |

**数据表**：无新增表，纯聚合视图。**接口**：`/api/v1/task-center/*`（list / stats / send）。

> ⚠️ 单一来源铁律：催办与逾期判据只允许来自 `backend/utils/dateflags.py`，task_center / requirement / reminder 三处禁止各自实现，否则数字漂移。

### 模块11：人员中台 Master Service（2026-07-28 新增，独立服务）

**定位**：全站**组织架构与人员信息的唯一数据源**。独立 FastAPI 服务（`services/master`，端口 `8001`，独立 alembic 迁移），主后端通过 `backend/utils/master_service.py` 代理调用，前端通过 `StaffSelect` 组件统一选人、`StaffAdminDrawer` 统一管理。

**功能清单**：

| 功能 | 说明 |
| :--- | :--- |
| 组织管理 | 组织/团队增删改查、排序、启用停用 |
| 人员管理 | 姓名、所属组织、邮箱、电话、角色备注、排序、启用停用 |
| 姓名解析 | `resolve_staff_emails`：把「中文姓名」解析为邮箱，供会议纪要/催办邮件收件人使用 |
| 全站选人 | 责任人 / 负责人 / 参会人 / 收件人等字段统一走 `StaffSelect`，不再手填 |

**数据表**：`pmwb_org`、`pmwb_staff`

```sql
CREATE TABLE pmwb_org (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(128) NOT NULL UNIQUE COMMENT '组织/团队名称',
    sort INT DEFAULT 0 COMMENT '排序号（小的在前）',
    enabled BOOLEAN NOT NULL DEFAULT 1 COMMENT '是否启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='基础数据-组织表';

CREATE TABLE pmwb_staff (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(64) NOT NULL COMMENT '姓名',
    org_id INT NOT NULL COMMENT '所属组织ID',
    email VARCHAR(255) COMMENT '邮箱（可空）',
    phone VARCHAR(64) COMMENT '电话（可空）',
    role_hint VARCHAR(128) COMMENT '角色/职责备注（可空）',
    sort INT DEFAULT 0 COMMENT '排序号',
    enabled BOOLEAN NOT NULL DEFAULT 1 COMMENT '是否启用',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_staff_name_org (name, org_id),
    FOREIGN KEY (org_id) REFERENCES pmwb_org(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='人员主数据表';
```

> ⚠️ 公共能力铁律：**禁止各模块自建人员表或硬编码收件人邮箱**，一律走人员中台。前端菜单入口为 `人员中台`（`/basic-data`）。

---

## 四、Obsidian 知识库归档方案（已确认）

### 4.1 新目录结构

```
D:\项目\知识图谱\
├── 00-工作台/              ← 保留（离线看板）
├── 00-产品圣经/            ← 新增（产品档案）
│   ├── 家客业务知识库/      ← 从 01-迁移
│   └── 政企业务知识库/      ← 从 01-迁移
├── 01-运营知识/            ← 新增
│   ├── Bug解决方案/
│   ├── 运营分析案例/
│   ├── 运维SOP/
│   └── 报表与分析/
├── 02-需求资产/            ← 新增
│   ├── 需求卡片/           ← 从 10-业务建设/迁移
│   ├── 需求方案/           ← 从 10-业务建设/迁移
│   ├── 操作手册/           ← 从 10-业务建设/迁移
│   └── 交付物/{req_id}/{ticket_no}/
├── 03-会议资产/{年份}/     ← 重构
├── 04-运营脚本库/          ← 新增
├── 05-个人知识/            ← 从 02-迁移
├── 06-学习知识/            ← 从 03-迁移
├── 07-日记与反思/          ← 从 04-迁移
├── 08-附件/               ← 重构（按类型分类）
├── 09-模板/               ← 从 07-迁移（编号对齐）
│   ├── 会议纪要模板.md     ← ⬆️ 升级（增加meeting_id/attendees字段）
│   ├── 产品档案模板.md     ← ⭐ 新增
│   ├── 运维SOP模板.md      ← ⭐ 新增
│   ├── Bug解决方案模板.md   ← ⭐ 新增
│   └── ...
├── 10-白板/               ← 从 12-迁移
├── 11-生活日常/            ← 从 13-迁移
├── 11-工作任务/            ← 从 08-迁移（待办双写后用,逐步废弃）
├── 使用指南.md
├── 标签体系.md
├── 知识图谱总览.md
└── copilot/               ← 保持
```

### 4.2 三阶段迁移策略

1. **上线前**：保持现有结构完全不变
2. **上线后**：新内容走新结构，存量不动
3. **逐步迁移**：旧会议纪要、旧 Bug 记录、旧附件按需迁移

### 4.3 关键文件模板要求

- **会议纪要模板**：必须包含 `meeting_id`、`title`、`start_time`、`attendees`、`summary`、`actions` 字段，便于系统解析。
- **产品档案模板**：必须包含 `product_id`、`category`、`target_user`、`value_proposition`、`key_features`、`related_systems` 字段。
- **运维 SOP 模板**：必须包含 `sop_id`、`scenario`、`steps`、`owner`、`review_cycle` 字段。
- **Bug 解决方案模板**：必须包含 `bug_id`、`issue_type`、`symptom`、`root_cause`、`solution`、`prevention` 字段。

---

## 五、通用功能需求

### 5.1 通知与提醒

- 待办到期提醒
- 会议开始前 15 分钟提醒
- 开发工单状态超期提醒
- 运营问题未闭环提醒
- 邮件发送成功/失败通知

### 5.2 搜索与筛选

- 全局搜索：跨需求、工单、运营问题、会议、知识条目搜索
- 列表筛选：按状态、优先级、时间、系统、分类等维度筛选
- 高级搜索：支持关键词组合、日期范围、关联对象筛选

### 5.3 数据导入导出

- 导入：从 Excel 批量导入待办、运营问题
- 导出：需求列表、工单列表、运营问题列表、会议列表导出为 Excel/PDF

### 5.4 系统配置

- 个人偏好设置：主题、默认首页、提醒方式
- 邮件模板配置：维护 `pmwb_*` 模板内容
- 联系人管理：维护常用联系人（SA、开发、测试、运营人员）
- 系统参数：Obsidian 路径、统一邮件中心地址、定时任务开关

---

## 六、非功能性需求

### 6.1 性能需求

- 首页看板加载时间 ≤ 2 秒
- 列表页查询 ≤ 1 秒（单次查询 ≤ 1000 条）
- 邮件发送异步处理，不阻塞前端操作

### 6.2 安全需求

- 本地部署，不对外暴露服务
- 数据库连接使用本地配置，不硬编码密码
- 文件上传限制类型和大小，避免安全风险

### 6.3 可用性需求

- 界面响应式，支持 1920×1080 及以上分辨率
- 操作有明确反馈：成功提示、失败提示、加载状态
- 支持键盘快捷键，提升操作效率

### 6.4 可维护性需求

- 代码模块化，业务逻辑与数据访问分离
- 统一错误处理和日志记录
- 数据库变更通过迁移脚本管理

### 6.5 可扩展性需求

- 预留 AI 分析接口，未来可接入大模型做智能分析
- 模块之间低耦合，新增模块不影响已有模块
- 邮件模板、知识模板可配置，不硬编码

---

## 七、现有资产清单

### 7.1 需求催办系统（基座）

| 资产 | 当前状态 | 复用方式 |
| :--- | :--- | :--- |
| MySQL yxtyg_db 数据库 | ✅ 运行中 | 直接复用（原 aicoding 库已统一至 yxtyg_db） |
| `sent_emails` 表（6000+条） | ✅ 运行中 | 需求数据源 |
| `sa_info` 表 | ✅ 运行中 | SA 联系人 |
| `email_records` 表 | ✅ 运行中 | 审计日志 |
| Node.js Web 服务（端口3000） | ⚠️ 已并入 PMWB | 网页功能并进 PMWB 前端（催办中心/邮件记录/集团需求），3000 可停 |
| Chrome 插件 | ✅ 已开发 | 需求/开发工单采集基座 |
| 统一邮件中心集成 | ✅ 已验证 | Python 端按此实现 |

### 7.2 统一邮件中心（公共服务）

| 服务 | 地址 |
| :--- | :--- |
| API | http://localhost:3210 |
| 邮件发送 | POST /api/send |
| 模板渲染 | POST /api/templates/:id/render |

### 7.3 Obsidian 知识库

| 资产 | 说明 |
| :--- | :--- |
| 知识库路径 | D:\项目\知识图谱 |
| 家客业务知识 | 21类产品，场景化分解 |
| 需求卡片 | 600+卡片，2021-2026连续记录 |
| 会议纪要 | 22篇 |
| 模板体系 | 15个模板，Templater集成 |
| 插件生态 | Calendar/Dataview/Templater/SmartConnections/Copilot |

---

## 八、附录

### 8.1 术语表

| 术语 | 说明 |
| :--- | :--- |
| PMWB | Product Manager Workbench，产品经理个人工作台 |
| req_id | 需求编号，唯一标识一个需求 |
| ticket_no | 开发工单编号，一个需求可能对应多个系统工单 |
| SA | System Analyst，系统分析人员/业务对接人 |
| Obsidian Vault | 本地 Markdown 知识库根目录 |

### 8.2 数据库表总览

| 表名 | 模块 | 说明 |
| :--- | :--- | :--- |
| `sent_emails` | 需求管理 | 已有，需求主数据 |
| `email_records` | 需求管理 | 已有，邮件发送记录 |
| `sa_info` | 通用 | 已有，SA 联系人 |
| `pmwb_requirement_ext` | 需求管理 | 新增，需求个人扩展字段 |
| `pmwb_requirement_evaluation` | 需求管理 | 新增，需求团队评估（key=sent_email_id），需求催办判据来源 |
| `pmwb_user_story` | 需求管理 | 新增，需求用户故事（草稿/定稿两态，验收标准与业务规则存 JSON） |
| `pmwb_dev_ticket` | 开发工单 | 新增，开发工单主表（含 `result_feedback` 处理结果反馈、`attachments` 附件 JSON） |
| `pmwb_dev_deliverable` | 开发工单 | 新增，交付物记录表 |
| `pmwb_dev_ticket_log` | 开发工单 | 新增，状态变更日志 |
| `pmwb_todo` | 待办中心 | 新增，待办任务表 |
| `pmwb_operation_issue` | 运营监控 | 新增，业务运营问题表 |
| `pmwb_meeting` | 会议管理 | 新增，会议表 |
| `pmwb_meeting_agenda` | 会议管理 | 新增，会议议题表（议题标题/背景说明/结论/分工，可排序） |
| `pmwb_meeting_attendee` | 会议管理 | 新增，会议参会人表 |
| `pmwb_meeting_action` | 会议管理 | 新增，会议行动项表 |
| `pmwb_knowledge_item` | 知识中心 | 新增，知识库条目索引表 |
| `pmwb_sql_script` | 知识中心 | 新增，SQL 脚本库表 |
| `pmwb_org` | 人员中台 | 新增，组织/团队表（全站人员唯一数据源） |
| `pmwb_staff` | 人员中台 | 新增，人员主数据表 |
| `pmwb_key_work` | 重点工作 | 新增，重点工作主表（category 区分 总部试点/年度任务/专题工作；status/priority 枚举；acceptance_criteria 存 JSON 字符串；progress 整数百分比） |
| `pmwb_key_work_goal` | 重点工作 | 新增，工作目标子表 |
| `pmwb_key_work_milestone` | 重点工作 | 新增，任务里程碑子表 |
| `pmwb_key_work_member` | 重点工作 | 新增，团队成员及分工子表 |
| `pmwb_key_work_monthly_plan` | 重点工作 | 新增，月度计划子表 |
| `pmwb_key_work_weekly_plan` | 重点工作 | 新增，周计划子表 |
| `pmwb_key_work_progress` | 重点工作 | 新增，工作进展子表 |
| `pmwb_key_work_member_task` | 重点工作 | 新增，成员待办子表（模块内专属，区别于全局待办） |
| `pmwb_key_work_deliverable` | 重点工作 | 新增，交付物记录表（落盘 Obsidian 09-重点工作） |

> 合计 **28 张表**（主后端 `backend/db/models.py`），与实际代码一一对应（2026-08-04 实测校准）。任务中心为纯聚合视图，无独立表。
>
> ⚠️ 改模型（增/改列）后必须先 `alembic upgrade head` 再起后端，否则 MySQL 1054 Unknown column → 前端 500。

### 8.3 版本历史

| 版本 | 日期 | 修改内容 | 作者 |
| :--- | :--- | :--- | :--- |
| v0.1 | 2026-07-13 | 初始版本，确立项目目标与模块划分 | 老大 |
| v0.2 | 2026-07-13 | 完善各模块功能清单、数据表设计、非功能性需求 | 老大 |
| v0.3 | 2026-07-16 | 整合需求催办系统全域进 PMWB；新增模块7 产品圣经、模块8 统一邮件催办；运营监控改 1表+5类(category)；数据库统一为 yxtyg_db | Vicky2号 |
| v0.4 | 2026-07-16 | 产品圣经功能清单对齐实际实现：业务切换器按 PRODUCT_BIBLE 配置加载、H2 目录树可折叠展开、关键字查询高亮、在线编辑写回 Obsidian | Vicky2号 |
| v0.5 | 2026-07-16 | 模块2 需求管理补「版本要求+跟踪状态三级预警」「团队评估只读+编辑」「关联工单进度面板」；pmwb_requirement_ext 补 version_required_date 列；需求列表交互优化（名称可点直达、整行点击展开） | UI Designer |
| v0.7 | 2026-08-04 | **对照实际代码全面校准**：架构图补人员中台 8001 与 5 端口拓扑；新增模块10 任务中心（6 类待办聚合）、模块11 人员中台（pmwb_org/pmwb_staff）；模块6 扩为知识中心四子页（补 SQL脚本库 pmwb_sql_script）；模块8 补邮件中心 5 子页；模块5 补议题表 pmwb_meeting_agenda（含背景说明）；模块2 补 pmwb_user_story；模块3 补结果反馈+附件；模块0 记录 DashboardV2 废弃决策与统计口径约定；8.2 表总览 22 → 28 张 | Vicky2号 |
| v0.6 | 2026-07-21 | 新增模块9 重点工作（总部试点/年度任务/专题工作全周期管理）；后端 9 张 pmwb_key_work* 表 + Alembic 迁移；KeyWorkOut 校验标准 JSON 字符串解析为数组、补 progress；修复成员待办/里程碑部分更新改用全可选 Update schema；前端 KeyWorkView 分类 Tab + KPI + 10 段详情抽屉 | Vicky2号 |

---

**文档状态**：v0.7 已对照实际代码全面校准（2026-08-04）——11 个功能模块、28 张表、5 端口服务拓扑与 `backend/db/models.py`、`frontend/src/router/index.js`、`backend/main.py` 逐项核对一致。

**关联文档**：
- `README.md` — 目录结构、启动方式、API 一览、关键约定
- `docs/COLLABORATIVE_DEV_WORKFLOW.md` — 协同开发规范（现行）
- `docs/开发工单模块设计.md` — 模块3 详细设计
- `docs/architecture_review_2026-07-25.md` — 架构评审与整改项
- `.workbuddy/tasks/TASKS.md` — 任务总表与批次记录

**下次评审重点**：生产监控子页（现标「建设中」）的功能范围；知识沉淀子页与 Obsidian 的落盘约定。
